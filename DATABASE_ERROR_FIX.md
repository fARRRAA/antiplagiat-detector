# 🔧 Исправление ошибки базы данных

## ❌ Проблема
```
The model backing the 'PlagiatContext' context has changed since the database was created. 
Consider using Code First Migrations to update the database.
```

## 📝 Причина
Модель базы данных изменилась (добавили `ProjectId` в `Document`), но существующая база данных не обновлена.

## ✅ Решения (выберите любое)

### 🚀 Решение 1: Автоматическое пересоздание (РЕКОМЕНДУЕТСЯ)

**Уже исправлено в коде!** Просто перезапустите приложение.

Изменения:
- ✅ `PlagiatContext` настроен на автоматическое пересоздание БД при изменении модели
- ✅ `DatabaseInitializer` проверяет совместимость и пересоздает БД при необходимости

### 🛠️ Решение 2: Ручное удаление через bat-файл

1. **Закройте приложение**
2. **Запустите:** `reset_database.bat`
3. **Перезапустите приложение**

### 💻 Решение 3: Удаление через SQL

1. **Откройте SQL Server Management Studio**
2. **Подключитесь к:** `(LocalDb)\MSSQLLocalDB`
3. **Выполните скрипт:** `delete_database.sql`
4. **Перезапустите приложение**

### 🔧 Решение 4: Через командную строку

```cmd
sqllocaldb stop MSSQLLocalDB
sqllocaldb delete-database MSSQLLocalDB PlagiatAssistant
sqllocaldb start MSSQLLocalDB
```

## 🎯 Ожидаемый результат

После применения любого решения:

1. ✅ **Приложение запустится** без ошибок
2. ✅ **База данных создастся** с новой схемой
3. ✅ **Тестовый документ загрузится** автоматически
4. ✅ **Все функции будут работать** корректно

## 📊 Что происходит при первом запуске

```
1. База данных не существует. Создаем...
2. База данных создана успешно!
3. Подключение к базе данных установлено!
4. Создание демо-данных...
5. Тестовый документ создан и загружен
```

## ⚠️ Важно

- **Данные не сохранятся** - база данных будет пересоздана
- **Это нормально** для разработки и тестирования
- **В продакшене** используйте Code First Migrations

## 🔄 Автоматическая обработка

Теперь приложение **автоматически**:
- ✅ Определяет изменения в модели
- ✅ Пересоздает базу данных при необходимости
- ✅ Создает тестовые данные
- ✅ Загружает тестовый документ

**Просто запустите приложение - все исправится автоматически! 🎉**

